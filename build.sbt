name := "Bounscale"

version := "0.0.1"

scalaVersion := "2.10.0"

publishTo := Some(Resolver.file("bounscale-play2",file("/root/work/play_work/maven/maven"))(Patterns(true, Resolver.mavenStyleBasePattern)))
